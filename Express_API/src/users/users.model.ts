// Define UserMediaList interface model
export interface StreamingService {
    id: number,
    userName: string,
    email: string,
    password: string,
    firstName: string,
    lastName: string,
}